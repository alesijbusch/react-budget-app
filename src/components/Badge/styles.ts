import styled from "styled-components";

const StyledBadge = styled.div`
  display: grid;
  grid-template-columns: auto 85px;
  padding: 32px 20px;
`;
const StyledBadgeInput = styled.input`
  font-style: normal;
  font-weight: 500;
  font-size: 20px;
  line-height: 24px;
`;
const StyledBadgeButton = styled.button`
  font-style: normal;
  font-weight: 500;
  font-size: 20px;
  line-height: 24px;
`;
export { StyledBadge, StyledBadgeInput, StyledBadgeButton };
